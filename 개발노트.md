# 개발 노트 — 이어서 작업할 때 읽는 문서

`README.md` 는 "어떻게 쓰는가"를 다룬다. 이 문서는 **"왜 이렇게 만들었는가"** 와
**"무엇이 검증됐고 무엇이 안 됐는가"** 를 남긴다. 다른 컴퓨터에서, 또는 시간이 지난 뒤
이어서 작업할 때 여기부터 읽으면 된다.

최초 작성: 2026-07-28

---

## 무엇을 만들었나

매 영업일 아침 06:40(KST) 텔레그램으로 시장 브리핑을 보낸다.
본문(지표 텍스트) + 차트 22장. GitHub Actions에서 실행되므로 PC를 켤 필요가 없다.

저장소: `animsam09-star/Daily-Express` (private)

---

## 핵심 제약 — 이걸 모르면 설계가 이해되지 않는다

**사내망에서 `api.telegram.org` 가 차단돼 있다.** TCP 연결은 열리지만 TLS 핸드셰이크가
30초 타임아웃된다(SNI 기반 도메인 차단). 반면 Yahoo Finance는 0.1초에 붙는다.

그래서 발송 주체를 사내 PC가 아니라 GitHub Actions로 뒀다. 이게 이 프로젝트에서
GitHub를 쓰는 유일한 이유다. "코드 관리"가 목적이 아니다.

부수 제약:
- 사내망은 SSL 검사 프록시가 있어 로컬 개발 시 인증서 검증이 깨진다
  → `SSL_VERIFY=0` 환경변수로 우회(프로덕션에는 절대 넣지 말 것)
- `C:\Users\DAOL\Desktop\김진영` 폴더는 사내 DRM 감시 대상이라
  생성한 docx/pdf/xlsx가 시간이 지나면 자동 암호화된다

---

## 데이터 소스를 이렇게 고른 이유

전부 실측으로 정했다. 추측이 아니다.

| 항목 | 소스 | 검증 |
|---|---|---|
| 미국 3대 지수, 섹터 11종, 원/달러 | Yahoo chart API | Dow/S&P/Nasdaq 등락률이 사용자 샘플과 소수점까지 일치 |
| 미국 국채 2/10/30년 **현재값** | CNBC(Tradeweb) | `US2Y 4.314%, open 4.322%` — 샘플의 `2yr 4.322%` 와 정확 일치 |
| 미국 국채 2개년 **히스토리** | 美 재무부 일별 CMT CSV | 차트용. CNBC는 히스토리를 안 준다 |
| 회사채 AA- 3년 / 국고채 3년 | 네이버 금융 시장지표 | `IRR_CORP03Y` = 4.57 로 샘플과 일치 |
| 스프레드 | 계산(회사채 − 국고3년) | 4.57 − 3.868 = 70.2bp, 샘플과 일치 → 산식 검증됨 |

**Yahoo에 2년물이 없어서** CNBC를 병용한다. `^TNX`(10y) `^TYX`(30y) 는 있지만
2년물 지수가 없고, `2YY=F`(선물)는 현물과 값이 다르다(4.051 vs 4.314).

차트는 재무부 CSV(D-1)로 그리고 마지막 점만 CNBC 실시간 값으로 덮어쓴다.
안 그러면 차트 제목과 본문 수치가 어긋난다(실제로 4.650 vs 4.635 로 어긋났던 버그).

---

## 넣지 못한 것과 그 이유

| 항목 | 왜 빠졌나 |
|---|---|
| 카드채 AA+ 3년, 캐피탈 AA-/A+ 3년 | 무료 공개 소스가 없다. KOFIA 채권정보센터 XML 엔드포인트는 살아 있으나(HTTP 200) 서비스명을 못 찾아 `MODULE ERROR` 상태. WebSquare 기반이라 JS 리버스가 필요하다 |
| 한국 CDS 프리미엄 | investing.com은 403, worldgovernmentbonds는 값이 JS 렌더라 서버 응답이 비어 있음(`---- basis points`). 근사치조차 안 나온다 |

여전채를 살리면 국내 금리 소수점 3자리 문제도 같이 해결된다(아래 한계 참조).

---

## 알려진 한계

- **국내 금리 정밀도**: 네이버는 소수 2자리까지만 공시한다. `-8.2bp` 가 `-8bp` 로,
  스프레드는 ±1bp 오차가 생긴다(실측: 71.0bp vs 실제 70.2bp). 3자리가 필요하면
  KOFIA 연동이 필수다
- **원/달러 정의**: Yahoo 글로벌 스팟이라 '서울 외환시장 종가'와 다르다. 아침
  브리핑 관점에선 간밤 뉴욕장이 반영된 쪽이 최신이라 판단해 그대로 뒀다
- **뉴스 요약**: 실제 요약 결과를 검증하지 못했다(개발 PC에 Anthropic 자격증명 없음).
  더미 키로 401을 받아 요청 형태만 확인했다. 401은 본문 전체 검증 전에 나오므로
  완전한 보증이 아니다 — **첫 실행 로그의 `[news]` 줄을 확인할 것**

---

## 설계 판단 기록

**뉴스 요약을 '등락 이유'라고 부르지 않는 이유.** 주가가 왜 움직였는지는 사후
해석이고 헤드라인이 곧 원인도 아니다. 자동으로 단정하면 그럴듯한 오답이 매일
아침 리서치 자료에 실린다. 그래서 프롬프트에 강제한다: 수집한 헤드라인에만
근거할 것, 설명이 안 되면 메모를 비울 것, 인과를 단정하지 말 것.
**빈 메모는 고장이 아니라 의도된 동작이다.**

실측상 야후 뉴스 검색은 노이즈가 많다(AAPL 검색에 SK하이닉스 기사가 섞인다).

**요약 인증이 두 경로인 이유.** `CLAUDE_CODE_OAUTH_TOKEN`(구독)이 있으면 Claude Code
헤드리스 모드를 쓰고, 없을 때만 `ANTHROPIC_API_KEY`(종량 과금)로 넘어간다.
구독자는 추가 비용이 들지 않는다. 토큰은 `claude setup-token` 으로 발급한다.

**섹터를 개별 차트로 뺀 이유.** 사용자가 "지표별 개별 차트 전부"를 선택했다.
22장이라 텔레그램 앨범(상한 10장)이 3개로 나뉜다. 일간 등락 막대와 2개년
상대성과 차트는 남겼다 — 개별 차트만으로는 섹터 간 상대 순위가 안 보인다.

---

## 섹터 메시지 설계 (2026-07-28 개편)

섹터 차트는 앨범이 아니라 **한 장씩 개별 전송**한다. 텔레그램 앨범은 사진별 캡션을
앨범 화면에 표시하지 않아서, 캡션이 본문인 섹터 차트가 사진을 눌러야만 보였다.

캡션 구성: 섹터 헤더(등락 + 1M/3M/6M/12M) → 종목 5개(회사명(티커)·주가·시총·
기간수익률) → 뉴스 최대 3개(등락 큰 종목 우선).

**뉴스는 '그날 일어난 사건'만 쓴다.** 두 종류를 명시적으로 배제한다.
- 결과만 있는 것: "상승 마감", "3% 하락" — 왜 움직였는지가 없다
- 상시 평가: "견조한 재무구조 부각", "밸류에이션 매력" — 어제도 오늘도 할 수 있는 말

수집 단계에서 시세·재무 데이터 페이지를 버리고 원인 연결어(after/beats/announces…)가
있는 기사를 앞으로 정렬한 뒤, 요약 프롬프트가 한 번 더 거른다. 근거가 없으면
메모를 비운다 — **빈 메모는 고장이 아니라 의도된 동작이다.**

뉴스 수집 경로: Finnhub(티커 직접) → 구글 뉴스 RSS(로이터·인베스팅·시킹알파) → 야후.

## 밟았던 함정 (재발 방지)

1. **step 의 `if` 에서 `secrets` 참조 불가** — `Unrecognized named-value: 'secrets'`
   로 워크플로 **파일 전체가 무효화**되고 Actions 목록에도 안 뜬다(Run workflow
   버튼이 사라짐). job 단위 `env` 로 받아서 `env.X != ''` 로 검사할 것
2. **텔레그램 캡션 정렬** — HTML 모드는 연속 공백을 접는다. 표는 `<pre>` 로 감싸야
   자리맞춤이 유지된다. 사명에 `&` 가 들어가는 종목(AT&T)이 있어 이스케이프 필수
3. **SSGA 티커 표기** — `BRK.B` 를 야후 심볼 `BRK-B` 로 변환해야 한다
4. **콘솔 인코딩** — 한글 출력 시 `PYTHONUTF8=1` 없으면 CP949 로 크래시한다
5. **차트 소스 혼용** — 히스토리와 현재값 출처가 다르면 제목과 본문이 어긋난다
6. **step 의 `if` 에서 `secrets` 참조 불가** — 워크플로 파일 전체가 무효화되고
   Actions 목록에서 사라진다(Run workflow 버튼도 없어짐). job 단위 `env` 로 받을 것
7. **캡션 길이는 표시 글자수 기준** — 텔레그램의 1024자 제한은 '엔티티 파싱 후'
   기준이라 태그와 링크 URL 은 세지 않는다. HTML 원문 길이로 재면 과대평가돼
   실제로는 들어갈 뉴스가 통째로 잘려나간다(998 로 계산됐지만 실제 618)
8. **티커 대조는 대소문자 구분** — `COP` 가 'cop shortage' 에, `KO` 가 복싱 기사에
   걸렸다. 기사에서 티커는 항상 대문자라 이게 유일한 구분 단서다
9. **복수 클래스 종목** — GOOGL/GOOG 처럼 같은 회사가 상위 5 중 두 자리를 먹는다.
   클래스 표기를 뗀 회사명으로 묶어 비중 큰 쪽만 남긴다
10. **야후 quote 배치 실패** — 배치 하나가 깨지면 그 종목들의 시가총액이 통째로
    빈다(차트 폴백은 시총을 못 준다). 배치를 작게 하고 재시도·개별조회로 메운다

---

## 다른 컴퓨터에서 이어서 작업하려면

```bash
git clone https://github.com/animsam09-star/Daily-Express.git
cd Daily-Express
pip install -r requirements.txt
python main.py --dry-run --outdir out    # 발송 없이 확인
```

사내망이면 `SSL_VERIFY=0` 을 앞에 붙인다.

시크릿은 저장소에 있으므로 코드만 받으면 된다. 로컬에서 실제 발송을 테스트하려면
`TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` 환경변수가 필요한데, 사내망에서는
텔레그램이 막혀 있어 어차피 안 된다.

---

## 남은 과제

- [ ] 첫 실행에서 뉴스 요약이 실제로 붙는지 확인 (`[news]` 로그)
- [ ] KOFIA 서비스명 확보 → 여전채 3종 + 국내 금리 소수 3자리
- [ ] 한국 CDS 대체 지표 검토 (한국은행 ECOS 외평채 가산금리 등)
- [ ] 발송 시각·차트 구성은 운영해보고 조정
